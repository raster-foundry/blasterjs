import { css } from "styled-components";

export default {
  heading: {
    {
      colors: {
        color: "red"
      },
      overrides: css`
        text-transform: uppercase;
      `
    }
  }
}
