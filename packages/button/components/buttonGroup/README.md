### Usage

```js
    const { Intent } = require('../../../core');
    // import { Intent } from '@blasterjs/core';

    <ButtonGroup>
        <Button intent={Intent.PRIMARY}>Button 1</Button>
        <Button intent={Intent.SECONDARY}>Button 2</Button>
    </ButtonGroup>
```
